exports.data = {
    dvalue:{},
    type:'object',
    description:`The data prop supports multiple data structures to accommodate different use cases. It accepts either:

An object where each key is a numeric value and each corresponding value is an object containing the item's data.
An array of objects, where each object represents an individual item.

This flexibility allows the component to work with both indexed object-based data and standard array-based collections, making it easier to integrate with different data sources and application architectures.`
}